import BaseComponent from '/components/BaseComponent.js';

export default class Footer extends BaseComponent {
    constructor(container) {
        super(container);
        // add filter inverted if dark mode
    }

    loaded() {
    }
}
